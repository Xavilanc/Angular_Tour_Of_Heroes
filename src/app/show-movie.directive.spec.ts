import { ShowMovieDirective } from './show-movie.directive';

describe('ShowMovieDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowMovieDirective();
    expect(directive).toBeTruthy();
  });
});
